three.js r160 (npm three@0.160.0), pinned locally so the site has no CDN dependency.
three.module.min.js is build/three.module.min.js; GLTFLoader.js and BufferGeometryUtils.js are
examples/jsm/* with their bare "three" imports rewritten to ./three.module.min.js. MIT licence: LICENSE.
